syntax = "proto2";

package ostorlab.scanner.proto.assets;

message AndroidMetadata {
  optional string package_name = 1;
}

message IOSMetadata {
  optional string bundle_id = 1;
}

message HarmonyOSMetadata {
  optional string bundle_name = 1;
}
