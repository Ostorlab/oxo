message device {
  required string device_id = 1;
}

message scan_start {
  required int32 scan_id = 1;
  required string platform = 2;
  required string package_name = 3;
  required bytes file = 4;
  repeated device devices = 5;
}