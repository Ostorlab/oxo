syntax = "proto2";

package ostorlab.agent.message.proto.v3.scan.file;

message ios {
    optional int32 scan_id = 1;
    optional bytes file = 2;
    optional string hash = 3;
    optional string platform = 4 [default = 'ios'];
}
