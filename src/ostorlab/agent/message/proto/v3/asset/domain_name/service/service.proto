syntax = "proto2";

package ostorlab.agent.message.proto.v3.asset.domain_name.service;

message Message {
  optional string name = 1;
  optional uint32 port = 2;
  optional string schema = 3;
  optional string state = 4;
}
