syntax = "proto2";

package ostorlab.agent.message.proto.v3.capture.artifact;

message Message {
    optional bytes content = 1;
    optional string description = 2;
    optional string type = 3;
    optional bool update_if_exist = 4 [default = false];
    optional string device_id = 5;
}
