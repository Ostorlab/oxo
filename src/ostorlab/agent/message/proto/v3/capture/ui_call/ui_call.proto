syntax = "proto2";

package ostorlab.agent.message.proto.v3.capture.ui_call;

message Message{
    optional string path = 1; 
    optional string dna = 2;
    optional bytes screenshot = 3;
    optional string parent_dna = 4;
}
