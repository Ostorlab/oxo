syntax = "proto2";

package ostorlab.agent.message.proto.v3.fingerprint.file.feature;

message Message {
  optional string feature_name = 1;
  optional string feature_description = 2;
  repeated string detail = 3;
}