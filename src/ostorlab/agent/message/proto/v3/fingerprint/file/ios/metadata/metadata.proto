syntax = "proto2";

package ostorlab.agent.message.proto.v3.fingerprint.file.ios.metadata;



message Permission {
    optional string name = 1;
    optional string usage_description = 3;
}

message Message {
    optional string bundle_id = 1;
    repeated Permission permissions = 2;
}
